# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
test/

# Operator config points at a private dataset — never commit it. The repo ships a generic
# `docusearch.example.yaml`; the real config is passed with `--config` and stays untracked.
/docusearch.yaml

# docusearch generated output — ALL generated output lands under tmp/ (CLAUDE.md).
# Exception: tmp/gates/ holds the gate files + evidence packs, which ARE the record
# checked into the repo (§15.4), so they are tracked.
tmp/*
!tmp/gates/
staging/
catalog.db
*.db
*.db-wal
*.db-shm
*.hnsw

# Downloaded corpora + models (large; not checked in — see corpora/MANIFEST.md)
corpora/data/
corpora/models/

# OS noise
.DS_Store
Thumbs.db
tmp/gates/evidence-phase6/artifacts/
tmp/gates/evidence-phase6/e2e/
